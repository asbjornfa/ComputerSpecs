import os
class Computer:
    def __init__(self, hostname, system, machine, kernel, compiler, processor, memory, disk):
        self.hostname = hostname
        self.system = system
        self.machine = machine
        self.kernel = kernel
        self.compiler = compiler
        self.processor = processor
        self.memory = memory
        self.disk = disk